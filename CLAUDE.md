# LeetCode 项目说明

## 项目结构

- `hot100/` — LeetCode Hot 100 题目，按分类子目录存放 `.cpp` 文件
- `README.md` — 刷题进度总表

## 更新规则

收到"更新表格"或类似指令时：

1. 扫描 `hot100/` 各子目录，列出所有 `.cpp` 文件
2. 对比 README.md 中对应分类的表格，找出缺失题目
3. 若 `hot100/` 下有 README.md 中尚未存在对应分类表格的子目录，则新建该分类的表格（含标题和表头）
4. 将缺失题目插入表格，**完成、日期、备注列留空**
5. 检查表格中完成列为空的条目，读取对应 `.cpp` 文件内容：若文件包含实际解题代码（函数体有逻辑，而非空壳），则将完成列更新为 ✅，日期填当天日期（**注意：文件存在 ≠ 已完成，需判断内容**）
6. 对每个分类的表格按以下规则重新排序：**先按难度（Easy → Medium → Hard），难度相同则按题号升序**
7. 更新顶部的进度计数（仅统计完成列有 ✅ 的行）

## 表格格式

```
| 题号 | 题目 | 完成 | 难度 | 日期 | 备注 |
|------|------|:----:|------|------|------|
```

- 完成：完成填 ✅，未完成留空
- 难度：从对应 `.cpp` 文件第 9 行提取，格式为 `* Easy/Medium/Hard (xx%)`，取第二个词
- 新增条目的完成、日期、备注列留空

## 用户背景

- **学历/经验**：大三，上过数据结构与算法设计课，有基本概念但印象不深，几乎没有算法刷题经验
- **刷题目标**：游戏客户端岗位面试准备（侧重算法题 + 现代 C++ 考察）
- **C++ 水平**：入门水平——熟悉指针、类的基本用法（不含多态/继承/虚函数）、动态内存、运算符重载；STL 容器用得很少；**对现代 C++（C++11/14/17）几乎没有接触**

## 解题帮助原则

1. **关联已做题目**：解释新题时，主动联系之前做过的类似题目，帮助建立知识网络
2. **STL 用到时顺带讲**：遇到 STL 容器或算法时，简要说明用法，不默认用户已知
3. **现代 C++ 特性**：遇到 `auto`、范围 for、lambda、智能指针、move 语义等时，顺带解释，结合游戏客户端面试场景说明为什么重要
4. **难度把控**：从直觉思路切入，再讲优化，不直接上最优解

## 语言

始终用简体中文回复。
